rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary dev rule: require authentication for all app data.
    // This prevents full public access while avoiding total outage from expiry.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
