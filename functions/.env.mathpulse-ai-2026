# Cloud Functions runtime environment for project: mathpulse-ai-2026
# Reassessment scheduler tuning
REASSESSMENT_INACTIVITY_DAYS=30
REASSESSMENT_SCAN_BATCH_LIMIT=300

# Optional workflow mode override (defaults to iar_only if omitted)
# IAR_WORKFLOW_MODE=iar_plus_diagnostic

# Optional backend override (defaults to HF Space URL in constants.ts)
# BACKEND_URL=https://deign86-mathpulse-api.hf.space
HF_TOKEN=
