# Ultrawork Notepad — Fix 4 tsc errors from typed routes

Started: 2026-06-01 17:08:58

## Plan (exhaustive, atomic)
1. Delegate 4-line fix to subagent
2. Run `npx tsc --noEmit` — expect clean
3. Persist to memory bank

## Scenarios (the contract)
- S1: `app/index.tsx` line 20: `router.replace('/teacher/dashboard')` must use typed-route form `router.replace('/(teacher)/dashboard')` — verify by tsc clean
- S2: `app/index.tsx` line 22: `router.replace('/admin/dashboard')` must use `router.replace('/(admin)/dashboard')` — verify by tsc clean
- S3: `app/(auth)/login.tsx` line 30: `router.replace(roleRoutes[...])` `string` lookup must produce typed route — fix `roleRoutes` map to use `/(student)/dashboard` etc. — verify by tsc clean
- S4: `app/(auth)/register.tsx` line 55: same fix as S3 — verify by tsc clean

## Now
- DONE

## Todo
- [x] Delegate fix — done (Sisyphus-Junior quick, 49s)
- [x] Verify tsc — VERIFIED_CLEAN_171057 (own re-run, not subagent)
- [ ] Memory bank — in progress

## Regression
- tsc clean: VERIFIED_CLEAN_171057 ✓
- All 4 errors fixed: app/index.tsx L20+L22, app/(auth)/login.tsx L25-30, app/(auth)/register.tsx L50-55
- Subagent used `as const` for roleRoutes to preserve literal types — clean pattern

## Findings
- `.expo/types/router.d.ts` auto-generated by `npx expo start --offline` (expo-router v5+ typed routes)
- Route groups `/(role)/...` are URL-invisible: `/(teacher)/dashboard` resolves to `/dashboard` at runtime
- 3 different `dashboard` files exist (student/teacher/admin), all collapse to same URL `/dashboard` — router disambiguates via current navigation stack
- Typed routes require `${'/(teacher)'}/dashboard` literal form (route group prefix preserved)
- `app/index.tsx` redirects based on role profile — must use role group form to disambiguate
- `roleRoutes` map type is `Record<string, string>` — must narrow to typed route union to satisfy `router.replace()`

## Learnings
- Always `npx expo start --offline` once to generate typed routes, THEN run `tsc --noEmit` — typed routes are now a hard gate
- Route group URL collapse is a common pitfall — keep `/(role)/...` in code, browser sees `/...`
